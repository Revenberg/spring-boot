server:
  port: 8888

spring: 
  cloud:
    config: 
      server: 
        git:
#          uri: file:///D:/git/configuration-service/configserver
           uri: https://github.com/Revenberg/configuration-service
management:
  endpoints:
    web:
      exposure:
       include: "*"

